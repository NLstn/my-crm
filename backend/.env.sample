# Backend environment configuration
# Copy to .env and adjust for your environment.

# Port the HTTP server listens on
PORT=8080

# Choose the data backend: "memory" (default) or "postgres"
DATA_BACKEND=memory

# PostgreSQL connection string, required when DATA_BACKEND=postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mycrm?sslmode=disable
